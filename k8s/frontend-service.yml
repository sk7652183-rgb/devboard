apiVersion: v1
kind: Service
metadata:
  name: devboard-frontend-service
  namespace: devboard
spec:
  selector:
    app: devboard-frontend
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4173
      nodePort: 30080
