apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv
  labels:
    app: devboard-postgres
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
   path: /mnt/devboard-postgres